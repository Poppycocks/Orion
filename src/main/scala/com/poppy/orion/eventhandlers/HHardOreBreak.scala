package com.poppy.orion.eventhandlers

      /*
@Handler()
object HHardOreBreak {
  @SubscribeEvent
  def BlockEvent(event: BreakEvent): Unit = {
    val player = event.getPlayer
    val block = event.block
    val meta = event.blockMetadata
    val world = event.world
    val x = event.x
    val y = event.y
    val z = event.z
    if (world.isRemote && block.isInstanceOf[BlockHardOre])
      if (meta>0)
        if (player == null) world.setBlock(x,y,z,block,meta-1, 2)
          else if (!player.capabilities.isCreativeMode) world.setBlock(x,y,z,block,meta-1, 2)
  }
}   */
